name: Print context

on:
  push:
    branches: [master, other]

jobs:
  triggers:
    name: Print info
    runs-on: ubuntu-latest
    steps:
      - name: GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: |
          echo "$GITHUB_CONTEXT"
